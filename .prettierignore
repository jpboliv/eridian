# Hand-tuned prompt content consumed verbatim as LLM instructions —
# not code style, and skills/speak/SKILL.md is parsed by exact
# HTML-comment delimiters (see scripts/lib/persona.js). Reformatting
# would risk silently altering injected persona text.
skills/
commands/

# Local-only design/plan docs and dev-workflow artifacts (gitignored,
# never committed — see .git/info/exclude and .superpowers/sdd/.gitignore).
# Excluded so local lint/format runs don't flag files that will never be
# shipped or seen by CI.
docs/superpowers/
.superpowers/

# Machine-generated by release-please using its own changelog formatting
# conventions (blank lines, bullet style) that don't match Prettier's
# config. release-please never runs Prettier on its output, so gating
# format:check on this file breaks CI on every single release.
CHANGELOG.md
